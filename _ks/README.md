# Karina Shimizu private site draft

This self-contained draft is intentionally kept under `_ks/`. It uses `noindex, nofollow` on every page and is not linked from the main site.

Open `index.html` locally to review the draft. The supplied portrait and CV PDFs remain in this folder as source material.
